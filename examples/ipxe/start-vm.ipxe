#!ipxe

set pxeserver gardenlinuxbootlocal.pxe

kernel http://${pxeserver}/rootfs.vmlinuz gl.url=http://${pxeserver}/root.squashfs gl.live=1 ip=dhcp console=ttyS0 gl.ovl=/:tmpfs
initrd http://${pxeserver}/rootfs.initrd
boot
